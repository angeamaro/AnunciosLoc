<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_profile"
        android:icon="@drawable/ic_menu_profile"
        android:title="Perfil" />
    <item
        android:id="@+id/menu_settings"
        android:icon="@drawable/ic_menu_settings"
        android:title="Definições" />
    <item
        android:id="@+id/menu_interests"
        android:icon="@drawable/ic_menu_interests"
        android:title="Interesses" />
    <item
        android:id="@+id/menu_policy"
        android:icon="@drawable/ic_menu_policy"
        android:title="Políticas" />

    <!-- Adiciona uma linha divisória -->
    <item android:title="">
        <menu>
            <item
                android:id="@+id/menu_logout"
                android:icon="@drawable/ic_menu_logout"
                android:title="Sair" />
        </menu>
    </item>
</menu>
